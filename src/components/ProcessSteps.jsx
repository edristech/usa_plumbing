const steps = [['01', 'Tell us what is happening', 'Share a few details so the right next step can be planned.'], ['02', 'Choose a service window', 'Use the contact process to coordinate a time that works.'], ['03', 'Get a clear path forward', 'A qualified professional assesses the issue and explains options.']]
export default function ProcessSteps() { return <div className="grid gap-8 md:grid-cols-3">{steps.map(([number, title, text]) => <div key={number} className="border-t-2 border-copper pt-5"><span className="font-display text-3xl font-semibold text-copper">{number}</span><h3 className="mt-5 font-display text-xl font-semibold text-ink">{title}</h3><p className="mt-2 text-sm leading-6 text-muted">{text}</p></div>)}</div> }