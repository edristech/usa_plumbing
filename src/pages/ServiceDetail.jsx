import { Check, CircleAlert } from 'lucide-react'
import { Link, useParams } from 'react-router-dom'
import CTASection from '../components/CTASection.jsx'
import FAQ from '../components/FAQ.jsx'
import ProcessSteps from '../components/ProcessSteps.jsx'
import SectionHeading from '../components/SectionHeading.jsx'
import ServiceGrid from '../components/ServiceGrid.jsx'
import ServiceHero from '../components/ServiceHero.jsx'
import { getService, getServiceDetail, services } from '../data/services.js'

export default function ServiceDetail() {
  const { service: slug } = useParams()
  const service = getService(slug)
  if (!service) return <main className="mx-auto max-w-7xl px-5 py-24"><h1 className="font-display text-4xl font-semibold">Service not found</h1><Link className="mt-5 inline-block text-copper" to="/services">Return to services</Link></main>
  const detail = getServiceDetail(service)
  const related = services.filter((item) => item.slug !== slug).slice(0, 3)
  const isEmergency = service.slug === 'emergency-plumbing'
  return <main><ServiceHero service={service} detail={detail} /><section className="mx-auto grid max-w-7xl gap-12 px-5 py-20 md:px-8 lg:grid-cols-2"><div><SectionHeading eyebrow="Overview" title={`A clear next step for ${service.title.toLowerCase()}.`} text={service.shortDescription} /><p className="mt-5 text-base leading-8 text-muted">Start with the symptoms you are seeing and the context around them. A professional assessment can help confirm the right service and explain what happens next.</p></div><div><SectionHeading eyebrow="Common problems" title="Signs it may be time to take a closer look." /><ul className="mt-8 grid gap-4">{detail.problems.map((item) => <li key={item} className="flex items-center gap-3 border-b border-line pb-4 text-sm text-ink"><Check size={17} className="text-copper" aria-hidden="true" />{item}</li>)}</ul></div></section><section className="bg-sand px-5 py-20 md:px-8"><div className="mx-auto max-w-7xl"><SectionHeading eyebrow="What the service involves" title="A clear scope, before the work begins." /><ul className="mt-10 grid gap-4 md:grid-cols-2">{detail.includes.map((item) => <li key={item} className="flex items-center gap-3 bg-white p-5 text-sm font-semibold text-ink"><Check size={17} className="text-copper" aria-hidden="true" />{item}</li>)}</ul></div></section><section className="mx-auto max-w-7xl px-5 py-20 md:px-8"><SectionHeading eyebrow="Benefits of professional service" title="The right service should leave you with more confidence." /><div className="mt-10 grid gap-4 md:grid-cols-3">{detail.benefits.map((item) => <div key={item} className="border-t-2 border-copper p-6 font-display text-xl font-semibold text-ink">{item}</div>)}</div></section><section className="bg-paper px-5 py-20 md:px-8"><div className="mx-auto max-w-7xl"><SectionHeading eyebrow="How it works" title="From first conversation to a clear next step." /><div className="mt-10"><ProcessSteps /></div></div></section><section className="mx-auto max-w-3xl px-5 py-20 md:px-8"><SectionHeading align="center" eyebrow="Questions" title="What homeowners often want to know." /><div className="mt-10"><FAQ items={[{ question: `How do I know if I need ${service.title.toLowerCase()}?`, answer: 'Share the symptoms and context with the company. A professional can confirm whether this is the right service.' }, { question: 'What happens after I reach out?', answer: 'The company can explain its response and scheduling process when you make contact.' }, { question: 'What information should I have ready?', answer: 'Note what you are seeing, when it started, and any steps already taken. Photos can also help when appropriate.' }]} /></div></section>{isEmergency && <section className="mx-auto max-w-7xl px-5 pb-12 md:px-8"><div className="flex items-center gap-3 border border-red-200 bg-red-50 p-5 text-sm font-semibold text-red-900"><CircleAlert size={18} aria-hidden="true" />For urgent plumbing issues, call 909-623-0033.</div></section>}<section className="bg-sand px-5 py-20 md:px-8"><div className="mx-auto max-w-7xl"><SectionHeading eyebrow="Related services" title="You may also need" /><div className="mt-10"><ServiceGrid items={related} /></div></div></section><CTASection title={isEmergency ? 'Need emergency plumbing help?' : `Ready to discuss ${service.title.toLowerCase()}?`} text={isEmergency ? 'Call 909-623-0033 for 24/7 Emergency Plumbing Service.' : 'Share a few details and start the conversation with USA Plumbing Service.'} /></main>
}
